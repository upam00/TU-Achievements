extends layout

block content

  h1 Delete Exam: #{exam.name}
  
  p Do you really want to delete this Exam?
  
  form(method='POST' action='')
      div.form-group
        input#id.form-control(type='hidden',name='id', required='true', value=exam._id )

      button.btn.btn-primary(type='submit') Delete